/// <reference types="react" />
import { SideSlideProps } from './types';
export declare const SideSlide: ({ show, onClose, children, direction, }: SideSlideProps) => JSX.Element;
export default SideSlide;
