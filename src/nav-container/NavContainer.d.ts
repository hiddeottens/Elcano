import React, { ReactNode } from 'react';
export interface NavContainerProps {
    children: ReactNode;
}
export declare const NavContainer: React.ForwardRefExoticComponent<NavContainerProps & React.RefAttributes<unknown>>;
export default NavContainer;
